[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "phase4-test-project"
version = "0.0.1"
requires-python = ">=3.11"

[project.scripts]
joke = "simple_flow_test_app.joke_teller:main"

[tool.pytest.ini_options]
pythonpath = [".", "src"]
